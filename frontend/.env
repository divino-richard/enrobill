# Base URL for the Laravel API.
# Left as "/api" so requests go through the Vite dev proxy (see vite.config.js).
# In production set this to the full backend URL, e.g. https://api.example.com/api
VITE_API_URL=/api
